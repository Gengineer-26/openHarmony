export class PhysicsConfig {
  static readonly PIXELS_PER_METER: number = 50;
  static readonly TIME_STEP: number = 1.0 / 60.0;
  static readonly VELOCITY_ITER: number = 8;
  static readonly POSITION_ITER: number = 3;
  static readonly MAX_BODIES: number = 80;
}
